
<form action="{{ path('_profile_edit_new') }}" {{ form_enctype(form) }} method="POST">
    {{form_widget(form)}}
    {{form_rest(form)}}
    <div>
        <input type="submit" value="{{ 'profile.edit.submit'|trans({}, 'FOSUserBundle') }}" />
    </div>
</form>
