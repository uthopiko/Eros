<h1>MstRolesEmpresa creation</h1>

<form action="{{ path('backend_maestros_roles_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('backend_maestros_roles') }}">
            Back to the list
        </a>
    </li>
</ul>
