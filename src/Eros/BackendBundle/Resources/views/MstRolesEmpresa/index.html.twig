<h1>MstRolesEmpresa list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Rol</th>
            <th>Codigo</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('backend_maestros_roles_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.rol }}</td>
            <td>{{ entity.codigo }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('backend_maestros_roles_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('backend_maestros_roles_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('backend_maestros_roles_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
