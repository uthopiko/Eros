<h1>MstTipoDescuento list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Descuento</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('backend_msttipodescuento_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.descuento }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('backend_msttipodescuento_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('backend_msttipodescuento_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('backend_msttipodescuento_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
